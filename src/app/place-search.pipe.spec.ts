import { PlaceSearchPipe } from './place-search.pipe';

describe('PlaceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
